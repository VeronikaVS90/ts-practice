// type Color = "red" | "green" | "blue";
// type Size = 4 | 8 | 16;

// const values = {
//   color: "green",
// } as const; //became readonly

// function paint(color: Color) {}

// paint(values.color);

// interface User {
//   readonly id: string;
// }

// type EventName = "click" | "change";

// type EventHandler = `on${EventName}`;

// type UserId = `user_id_${string}`;
